//Retrieving an user information
import connectToDB from "../../../db/mongodb";

export default async function handler(req, res) {
  if (req.method === "GET") {
    try {
        
    } catch {}
  }
}
